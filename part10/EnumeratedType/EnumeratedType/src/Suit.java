public enum Suit {
    /**
    An enum is a class and follows same regulations.

    Having it on its own file is exactly like moving an 
    inner class in a separate file, nothing more nor less. 
    So yes you can move it inside one of the class and be able to access it from outside 
    with OuterClass.ScaleName syntax.
 */
    DIAMOND, SPADE, CLUB, HEART;

}
